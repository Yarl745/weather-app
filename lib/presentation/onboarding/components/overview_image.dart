import 'package:flutter/material.dart';

class OverviewImage extends StatelessWidget {
  const OverviewImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
